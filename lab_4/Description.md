# 📁 Система контроля доступа к файлам

## 📋 Описание проекта

**Система контроля доступа к файлам** — это консольное приложение на Scala, реализующее **дискреционную модель доступа**. Проект демонстрирует основные принципы информационной безопасности: аутентификацию, авторизацию и управление правами доступа к файлам.

### Основные возможности:

1. **🔐 Аутентификация и авторизация**
    - Регистрация новых пользователей
    - Вход в систему под пользователем
    - Вход под администратором (полные права)
    - Хеширование паролей (SHA-256)

2. **📄 Управление файлами**
    - Создание, чтение, запись, удаление файлов
    - Дописывание в конец файла
    - Очистка содержимого файла
    - Просмотр информации о файле

3. **👥 Контроль доступа**
    - Владелец файла может предоставлять права другим пользователям
    - Четыре типа прав: чтение (R), запись (W), дописывание (A), очистка (D)
    - Администратор имеет полный доступ ко всем файлам

4. **💾 Сохранение состояния**
    - Автоматическое сохранение данных
    - Загрузка состояния при запуске
    - CSV-файлы для хранения пользователей, файлов и прав доступа

## 🚀 Запуск проекта

### Предварительные требования:
- **Java JDK 8+**
- **Scala 2.13+**
- **sbt** (Scala Build Tool)

### Структура проекта:
```
lab_4/
├── src/main/scala/
│   ├── Main.scala                 # Точка входа
│   ├── AuthService.scala          # Сервис аутентификации
│   ├── FileService.scala          # Сервис работы с файлами
│   ├── AccessControlService.scala # Сервис контроля доступа
│   ├── SecurityUtils.scala        # Утилиты безопасности
│   └── Models.scala               # Модели данных
└── src/main/resources/files/      # Директория для данных
```

### Шаги запуска:

1. **Клонирование/копирование** проекта в рабочую директорию

2. **Запустить скрипт(не везде будет работать!!!)**
```bash

cd ../
chmod +x run_lab_4.sh
./run_lab_4.sh
```
> *Примечание:* Скрипт на Ubuntu запускается корректно
## 📊 Файлы данных

При первом запуске создаются директории и файлы:
- `files/users.csv` — данные пользователей (логин, хеш пароля, роль)
- `files/files.csv` — метаданные файлов (имя, владелец)
- `files/access_rights.csv` — права доступа пользователей к файлам
- `files/` — физическое хранение файлов

## 👑 Доступ по умолчанию

**Администратор:**
- Логин: `admin`
- Пароль: `admin123`

## 🎮 Основные команды в системе

### Меню входа:
- `register` — регистрация нового пользователя
- `login` — вход под пользователем
- `admin` — вход как администратор
- `help` — справка по командам
- `exit` — выход из программы

### Главное меню:
- **1-9** — операции с файлами (создание, чтение, запись и т.д.)
- `stats` — статистика системы
- `save` — принудительное сохранение данных
- `help` — справка по командам
- `0` — выход из системы

## ⚠️ Важные особенности

1. **Валидация имен:**
    - Имена пользователей: 3-20 символов, только буквы, цифры, подчеркивание
    - Имена файлов: запрещены символы `/ \ : * ? " < > |`

2. **Пароли:**
    - Минимум 6 символов
    - Хранятся в виде SHA-256 хешей

3. **Автосохранение:**
    - Данные сохраняются после каждой операции
    - Загружаются при запуске приложения

4. **Права доступа:**
    - Владелец файла имеет полные права
    - Администратор имеет доступ ко всем файлам
    - Права наследуются: запись включает чтение, удаление включает очистку
---
